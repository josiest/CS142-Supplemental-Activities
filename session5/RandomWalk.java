import java.awt.Graphics;
import java.awt.Color;

public class RandomWalk {
    public static final int walkerSize = 5;

    /** Draw a square that moves one unit in a random direction every "frame".
     *
     * The square may not go out of the bounds of the screen.
     * The square must only move in increments of one "unit" or walkerSize.
     * The square must stop moving after a set amount of "frames."
     *  You should decide how many frames the walker moves for.
     */
    public static void draw(Graphics g, int width, int height) {
    }
}
