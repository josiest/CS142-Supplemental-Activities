import java.awt.Graphics;
import java.awt.Color;
import java.awt.Rectangle;

public class Rects {

    /* This method sould animate two rectangles moving toward each other from
     * opposite ends of the screen.
     *
     * The two rectangles should be colored differently, and when they
     * intersect, they should change color.
     *
     * Bonus points if the intersecting rectangle also changes to a different
     * color than either of the two rectangles.
     */
    public static void draw(Graphics g, int width, int height) {
    }
}
